{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^16.0.2",
    "purescript": "^0.14.0",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.0",
    "spago": "^0.20.9"
  }
}
